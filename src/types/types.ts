export type Types = {
    id: number,
    name: string,
    image: string,
    manufacturer: string,
    releaseYear: number,
    screenSize: number,
    country: string,
    memory: number,
    refreshRate: number,
    nfc: boolean,
    esimSupport: boolean,
    wirelessCharging: boolean,
    price: number
}
